# Copie ce fichier en .env et adapte les valeurs (le .env n’est pas versionné).
# macOS / Linux : voir README pour charger ces variables avant de lancer le binaire.
# Windows : utilise PowerShell ($env:PORT=...) ou les variables système.

PORT=8080